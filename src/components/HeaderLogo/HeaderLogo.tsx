import styles from "./HeaderLogo.module.scss";

const HeaderLogo = () => {
  return <div className={styles.container}>
    <h1>Frontend <span>portfolio</span></h1>
  </div>;
};

export default HeaderLogo;
